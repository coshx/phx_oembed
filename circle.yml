dependencies:
  pre:
    - ./circle-prepare.sh

  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - ./circle-tests.sh
    - mocha -r mock-local-storage --compilers js:babel-register --recursive test/js
